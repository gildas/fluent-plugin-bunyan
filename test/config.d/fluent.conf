# Listen to HTTP traffic on port 3000
<source>
  @type http
  port 3000
  bind 0.0.0.0
  <parse>
    @type bunyan
  </parse>
</source>

# Dump analyzed data to stdout
<match **>
  @type stdout
</match>